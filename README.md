# AWS-Least-Privilege-IAM-Design-Lab
AWS least-privilege IAM design lab. Demonstrates scoped IAM roles for an application (EC2 runtime), CI/CD pipeline (GitHub Actions OIDC deploy), and developer debug access — each restricted to the minimum permissions required for its function. Includes trust policies, inline permission policies, and role justifications.
